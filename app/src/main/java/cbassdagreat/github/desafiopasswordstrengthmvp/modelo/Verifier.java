package cbassdagreat.github.desafiopasswordstrengthmvp.modelo;

public class Verifier {

  /**  private String pass;
    private boolean tieneMayus(String pass) {
        boolean aux = true;
        for (int i = 0; i < pass.length();i++){
            if (Character.isDigit(pass.charAt(i++)))
   }
**/

//public boolean tieneMayusculas(String pass) {
// for (int i = 0; i < pass.length(); i++) {
// if (Character.isUpperCase(pass.charAt(i))) {
// return true;
// }
// }
// return false;
// }
//
// public boolean tieneNumeros(String pass) {
// for (int i = 0; i < pass.length(); i++) {
// if (Character.isDigit(pass.charAt(i)))
// {
// return true;
// }
// }
// return false;
// }
 /**   }

    private boolean conNums(String pass){
        boolean aux = true;
    }

    private boolean longitudPass(String pass){
        boolean aux;
        if (pass.length() <= 5){
            aux = false;
        }
        else
        {
            aux = true;
        }

        return = aux;

    }
**/
}
